import { Telegram as TelegramMUI } from "@mui/icons-material"

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const Telegram = (props: any) => {
  return <TelegramMUI {...props} />
}
