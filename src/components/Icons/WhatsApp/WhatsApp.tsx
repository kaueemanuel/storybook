import { WhatsApp as WhatsAppMUI } from "@mui/icons-material"

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const WhatsApp = (props: any) => {
  return <WhatsAppMUI {...props} />
}
