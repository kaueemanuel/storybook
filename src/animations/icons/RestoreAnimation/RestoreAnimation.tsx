import { SvgIconProps } from "@mui/material"

import { SvgIconStyled } from "./RestoreAnimation.styles"

const RestoreAnimation = (props: SvgIconProps) => {
  return <SvgIconStyled {...props} />
}

export default RestoreAnimation
